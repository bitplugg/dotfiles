#!/usr/bin/env bash
# Launch wallpaper picker via AGS
cd ~/.config/ags

INSTANCE="ags-wallpaper-picker"

# Toggle: if running, close it
if ags list 2>/dev/null | grep -q "$INSTANCE"; then
    ags quit "$INSTANCE" 2>/dev/null
    exit 0
fi

ags run wallpaper-picker.tsx "$@" &
disown
